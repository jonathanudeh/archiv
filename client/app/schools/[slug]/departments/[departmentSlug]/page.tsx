// /school/[slug]/department/[departmentSlug]/
function departmentPage() {
  // console.log("Department page for slug: ", slug);
  return (
    <main className="min-h-screen bg-white">
      <div className="">
        This route doesn't have a page yet, but it will be for a specific
        department.
      </div>
    </main>
  );
}

export default departmentPage;
